services:
  orchestration:
    build: .
    ports:
      - "3100:3100"
    env_file:
      - .env
    volumes:
      - orchestration-data:/app/data
    restart: unless-stopped

volumes:
  orchestration-data:
